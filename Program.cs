
namespace PersonalPlatformer;

class Program
{
    static void Main(string[] args)
    {
        private const float SCREEN_HEIGHT = 1200;
        private const float SCREEN_WIDTH = 800;
        using (var window = new RenderWindow)
}
}